import { StyleSheet } from 'react-native';

export const BurgerStyles = StyleSheet.create({
    container: {
        width: 30,
        height: 30,
        justifyContent: 'center',
        gap: 8
    },
    line: {
        width: 30,
        height: 2,
        backgroundColor: '#bbb'
    },

});
